source 'https://rubygems.org'

# Use the gem from this repo (pulls in its runtime deps: redcarpet, rexml,
# rubyzip) instead of the published release, so samples track local changes.
gemspec path: '../..'
